export type Item = {
  id: string;
  partNumber: string;
  serialNumber: string;
  purchaseOrder: string;
  description: string;
  category: string;
  price: number;
  quantity: number;
  status: string;
  repairOrderNumber: string;
  condition: string;
  inventoryID: string;
};
